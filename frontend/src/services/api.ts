import type {GoogleCalendarItem,GoogleCalendarSource,GoogleCalendarStatus,GoogleSourceInput,GoogleSyncResult,MemberRegistration,Summary,Task,TaskInput,TaskPage,User} from '../types';
const request=async<T>(path:string,init?:RequestInit):Promise<T>=>{const response=await fetch(`/api${path}`,{credentials:'include',headers:{'Content-Type':'application/json'},...init});if(!response.ok){const body=await response.json().catch(()=>({detail:'Erro inesperado'}));throw new Error(body.detail??'Erro inesperado')}if(response.status===204)return undefined as T;return response.json()};
export const api={login:(email:string,password:string)=>request<User>('/auth/login',{method:'POST',body:JSON.stringify({email,password})}),registerMember:(data:MemberRegistration)=>request<User>('/auth/register-member',{method:'POST',body:JSON.stringify(data)}),logout:()=>request<void>('/auth/logout',{method:'POST'}),me:()=>request<User>('/auth/me'),tasks:(query='')=>request<TaskPage>(`/tasks${query}`),summary:()=>request<Summary>('/dashboard/summary'),create:(data:TaskInput)=>request<Task>('/tasks',{method:'POST',body:JSON.stringify(data)}),update:(id:number,data:Partial<TaskInput>)=>request<Task>(`/tasks/${id}`,{method:'PATCH',body:JSON.stringify(data)}),remove:(id:number)=>request<void>(`/tasks/${id}`,{method:'DELETE'}),googleStatus:()=>request<GoogleCalendarStatus>('/integrations/google-calendar/status'),googleConnect:()=>request<{authorization_url:string}>('/integrations/google-calendar/connect'),googleCalendars:()=>request<GoogleCalendarItem[]>('/integrations/google-calendar/calendars'),googleCreateSource:(data:GoogleSourceInput)=>request<GoogleCalendarSource>('/integrations/google-calendar/sources',{method:'POST',body:JSON.stringify(data)}),googleUpdateSource:(id:number,data:Partial<Omit<GoogleSourceInput,'calendar_id'>>)=>request<GoogleCalendarSource>(`/integrations/google-calendar/sources/${id}`,{method:'PATCH',body:JSON.stringify(data)}),googleDeleteSource:(id:number)=>request<void>(`/integrations/google-calendar/sources/${id}`,{method:'DELETE'}),googleSync:(sourceId?:number)=>request<GoogleSyncResult>(`/integrations/google-calendar/sync${sourceId?`?source_id=${sourceId}`:''}`,{method:'POST'}),googleDisconnect:()=>request<void>('/integrations/google-calendar/disconnect',{method:'POST'})};

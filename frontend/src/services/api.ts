import type {Summary,Task,TaskInput,TaskPage,User} from '../types';
const request=async<T>(path:string,init?:RequestInit):Promise<T>=>{const response=await fetch(`/api${path}`,{credentials:'include',headers:{'Content-Type':'application/json'},...init});if(!response.ok){const body=await response.json().catch(()=>({detail:'Erro inesperado'}));throw new Error(body.detail??'Erro inesperado')}if(response.status===204)return undefined as T;return response.json()};
export const api={login:(email:string,password:string)=>request<User>('/auth/login',{method:'POST',body:JSON.stringify({email,password})}),logout:()=>request<void>('/auth/logout',{method:'POST'}),me:()=>request<User>('/auth/me'),tasks:(query='')=>request<TaskPage>(`/tasks${query}`),summary:()=>request<Summary>('/dashboard/summary'),create:(data:TaskInput)=>request<Task>('/tasks',{method:'POST',body:JSON.stringify(data)}),update:(id:number,data:Partial<TaskInput>)=>request<Task>(`/tasks/${id}`,{method:'PATCH',body:JSON.stringify(data)}),remove:(id:number)=>request<void>(`/tasks/${id}`,{method:'DELETE'})};
